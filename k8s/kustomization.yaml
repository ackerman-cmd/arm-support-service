  apiVersion: kustomize.config.k8s.io/v1beta1
  kind: Kustomization

  namespace: arm-support

  resources:
    - namespace.yaml
    - configmap.yaml
    - secret.yaml
    - deployment.yaml
    - service.yaml
    - ingress.yaml

  images:
    - name: arm-support-service
      newName: ghcr.io/ackerman-cmd/arm-support-service
      newTag: latest
